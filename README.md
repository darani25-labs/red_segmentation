# red_segmentation
# 🔴 Red Object Segmentation using HSV Thresholding

This project segments red-colored objects from an image, such as a festival photograph, using the Hue, Saturation, Value (HSV) color space and thresholding with OpenCV. The output is a binary mask highlighting the red regions.

## ⚙️ Prerequisites

You need Python installed on your system. The following libraries are required:

* `opencv-python`
* `numpy`

To install the dependencies, use the `requirements.txt` file:

```bash
pip install -r requirements.txt
